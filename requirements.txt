# ============================================================
# FIT5217 A2 — 本地开发完整依赖
# 用法: pip install -r requirements.txt
# Python 版本建议 3.10+(与 Colab 对齐)
# ============================================================

# ---- 深度学习核心 ----
torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.28.0
datasets>=2.18.0
sentencepiece>=0.2.0

# ---- 评估指标 ----
nltk>=3.8.1
sacrebleu>=2.4.0
bert-score>=0.3.13

# ---- Task 3 RAG ----
rank_bm25>=0.2.2
sentence-transformers>=2.6.0
faiss-cpu>=1.7.4

# ---- LLM API 客户端(免费 tier) ----
openai>=1.20.0          # 兼容 Groq / OpenRouter / NVIDIA NIM 的 OpenAI 风格接口
python-dotenv>=1.0.0

# ---- 数据处理与可视化 ----
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.13.0
tqdm>=4.66.0
scikit-learn>=1.3.0     # 划分小样本、混淆矩阵、metrics 辅助

# ---- 配置与输出 ----
pyyaml>=6.0             # configs/ 下的超参 YAML
openpyxl>=3.1.0         # 写 generated_<sid>.xlsx

# ---- Notebook ----
jupyter>=1.0.0
ipykernel>=6.29.0
